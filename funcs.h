#pragma once
#include <string>
#include <regex>
#include <iostream>
#include "Boost.h"
using namespace std;
int checkInt(std::string str);
double checkDouble(std::string str);

std::string checkStr(std::string str);

void menu();

