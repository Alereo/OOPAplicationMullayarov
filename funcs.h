#pragma once
#include <string>
#include "Student.h"
#include <regex>
#include "Group.h"
int checkInt(std::string str);
double checkDouble(std::string str);

std::string checkStr(std::string str);

void menu();

